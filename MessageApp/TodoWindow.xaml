<Window x:Class="MessageApp.TodoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MessageApp"
        mc:Ignorable="d"
        Title="Task Manager" Height="550" Width="650"
        Background="#1E1E2E">

    <Window.Resources>
        <SolidColorBrush x:Key="CardBrush" Color="#2D2D3A"/>
        <SolidColorBrush x:Key="InputBrush" Color="#3D3D4A"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#7C4DFF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#A0A0B0"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF6B6B"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="#4A4A5A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <DataTemplate x:Key="TodoItemTemplate">
            <Border Background="{StaticResource CardBrush}" 
                    CornerRadius="8" Margin="5,3" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <CheckBox Grid.Column="0" 
                              IsChecked="{Binding IsCompleted}"
                              VerticalAlignment="Center"
                              Click="TodoCheckBox_Click"
                              Tag="{Binding Id}"/>

                    <StackPanel Grid.Column="1" Margin="10,0">
                        <TextBlock Text="{Binding Title}" 
                                   FontWeight="Bold" 
                                   FontSize="14"
                                   Foreground="{StaticResource TextPrimary}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                            <Setter Property="TextDecorations" Value="Strikethrough"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding Description}" 
                                   FontSize="11" 
                                   Foreground="{StaticResource TextSecondary}"
                                   TextWrapping="Wrap"
                                   MaxHeight="40"/>
                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                            <TextBlock FontSize="10" Foreground="{StaticResource WarningBrush}">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="📅 Due: {0:yyyy-MM-dd}">
                                        <Binding Path="DueDate"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <TextBlock FontSize="10" Foreground="{StaticResource TextSecondary}" Margin="10,0,0,0">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="👤 By: {0}">
                                        <Binding Path="CreatedBy"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="✏️" Width="35" Click="EditTodo_Click" Tag="{Binding Id}"/>
                        <Button Content="🗑️" Width="35" Click="DeleteTodo_Click" Tag="{Binding Id}"
                                Background="{StaticResource DangerBrush}" Margin="5,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="📋 Task Manager" FontSize="22" FontWeight="Bold" 
                   Foreground="{StaticResource TextPrimary}" Margin="0,0,0,15"/>

        <!-- Форма додавання задачі -->
        <Border Grid.Row="1" Background="{StaticResource CardBrush}" 
                CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Text="TITLE" Foreground="{StaticResource TextSecondary}" FontSize="11" FontWeight="SemiBold"/>
                <TextBox x:Name="TitleBox" Grid.Row="1" Margin="0,5,0,10"/>

                <TextBlock Grid.Row="2" Text="DESCRIPTION" Foreground="{StaticResource TextSecondary}" FontSize="11" FontWeight="SemiBold"/>
                <TextBox x:Name="DescriptionBox" Grid.Row="3" Height="60" TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto" Margin="0,5,0,0"/>

                <StackPanel Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" Margin="15,0,0,0">
                    <TextBlock Text="DUE DATE" Foreground="{StaticResource TextSecondary}" FontSize="11" FontWeight="SemiBold"/>
                    <DatePicker x:Name="DueDatePicker" Width="130" Height="30" Margin="0,5,0,0"/>
                </StackPanel>

                <Button Grid.Row="3" Grid.Column="1" Content="➕ ADD TASK" 
                        Click="AddTodo_Click" Width="130" Height="35" 
                        VerticalAlignment="Bottom" Margin="15,0,0,0"/>
            </Grid>
        </Border>

        <!-- Список задач -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="TASKS" FontSize="13" FontWeight="Bold" 
                       Foreground="{StaticResource TextSecondary}" Margin="0,0,0,10"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="TodoList" ItemTemplate="{StaticResource TodoItemTemplate}"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>